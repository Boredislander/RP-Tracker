JWT_SECRET=change-me-to-a-long-random-string
PORT=3000
NODE_ENV=production

# Tracker Network API — get your key at tracker.gg/developers
# Without this, the auto-sync feature is disabled (manual logging still works)
TRN_API_KEY=
